services:
  - type: web
    name: stock-prediction-1
    env: python
    region: oregon
    plan: free
    pythonVersion: 3.11.0
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
